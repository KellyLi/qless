CREATE TABLE qless (
  arrival_time INT,
  weekday VARCHAR(3),
  flow_rate INT,
  queue_length INT,
  doctor INT,
  appt_time INT,
  is_walk_in BOOLEAN,
  num_doctors INT,
  month INT,
  seen_time INT,
  estimated_wait_time INT,
  timestamp DATETIME,
  has_been_fitted BOOLEAN
);
